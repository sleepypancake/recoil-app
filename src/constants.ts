import { Element } from "./types";

export const defaultElement:Element = {
    style: {
        position: {
            top: 100,
            left: 100
        },
        size: {
            width: 50,
            height: 50
        }
    }
}